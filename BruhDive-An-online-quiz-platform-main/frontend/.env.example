# Optional: Production Backend API URL (if deploying backend separately on Render/Railway/Vercel)
# If not set, it defaults to http://localhost:5000 in dev and live backend in production
VITE_BACKEND_URL=https://your-backend-api.onrender.com
